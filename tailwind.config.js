/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./templates/**/*.html'],
  presets: [],
  darkMode: 'media',
  theme: {
    container: {
      center: true,
    },
  },
  plugins: [],
}
